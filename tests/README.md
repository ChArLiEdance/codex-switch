# Tests

This directory is reserved for integration and simulated-environment tests.

Planned coverage:

- Profile metadata validation
- Credential-store abstraction
- Timestamped backup
- Rollback
- Switch transaction state machine
- Mock Codex CLI, VS Code, and Desktop auth directories without real tokens

